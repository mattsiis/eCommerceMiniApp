<view class='counter'>
  <text class="icon {{quantity == 1 ? '.grey' : '' }}"  data-num='{{data.quantity}}' bindtap="minusCount" >-</text>
  <view class='number'>{{quantity}}</view>
  <text class='icon' bindtap="addCount" data-num='{{data.quantity}}'>+</text>
</view>